import React from 'react'

export const Group: React.FC = () => {
  return (
    <div className="component-layout">
      <ul>
        <li>项目大厅 这里展示的是想找人一起的 加一个按钮可以发布</li>
        <li>应该是要同步的home</li>
        <li>request判断 如果项目创立就加到每个人的home里</li>
      </ul>
    </div>
  )
}
